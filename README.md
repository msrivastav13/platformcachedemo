# Salesforce App

This is a simple lightning component that demonstrates how one can use platform cache to gain performance.

The component uses Schema Describe to get all objects list.

Also there is sample code to show how to use CacheBuilderInterface to store a SOQL or Describe Schema.